<%= form_with(model: baby, local: true) do |form| %>
  <% if baby.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(baby.errors.count, "error") %> prohibited this baby from being saved:</h2>

      <ul>
      <% baby.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <p>Baby</p>
  
  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>
  
  <p>Breatsfeeding</p>
  
  <%= form.fields_for :breatsfeeding do |breatsfeeding_form| %>
    <%= breatsfeeding_form.label :cantidad %>
    <%= breatsfeeding_form.text_field :cantidad %>
  <% end %>

  <%= form.fields_for :breatsfeeding do |breatsfeeding_form| %>
    <%= breatsfeeding_form.label :tiempo %>
    <%= breatsfeeding_form.text_field :tiempo %>
  <% end %>
  
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>